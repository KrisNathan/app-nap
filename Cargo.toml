[package]
name = "app-pause"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
tokio = { version = "1.52.1", features = ["macros", "rt-multi-thread"] }
zbus = { version = "5", features = ["blocking-api"] }
